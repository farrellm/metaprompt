Start your response directly with the <Inputs> block. Do not include any preamble, commentary, or text before it.
{{#VARS}}
Your <Inputs> block, and the prompt template you write in <Instructions>, must use exactly these input variables and no others: {{{VARS}}}.
{{/VARS}}
{{#CONTROLS}}
The existing prompt contains these mustache tags: {{{CONTROLS}}}. Reproduce every one of them verbatim, unchanged, in your <Instructions> block.
{{/CONTROLS}}
